{
  "dependencies": {
    "lerna": "^4.0.0"
  },
  "name": "punkbeer",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "components/*"
    ]
  },
  "scripts": {
    "start": "lerna exec --scope @iwann/web -- yarn start"
  }
}
