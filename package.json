{
  "dependencies": {
    "craco": "^0.0.3",
    "eslint-plugin-import": "^2.25.4",
    "lerna": "^4.0.0"
  },
  "name": "@iwann/punkbeer",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "components/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**",
      "**/react-native-typescript-transformer"
    ]
  },
  "scripts": {
    "bootstrap": "lerna clean --yes && rimraf node_modules && yarn && lerna bootstrap",
    "startweb": "lerna run --scope @iwann/web start",
    "startmobileserv": "lerna run --scope @iwann/mobile start",
    "startapp": "lerna run --scope @iwann/mobile android"
  }
}
