{
  "dependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "name": "@iwann/punkbeer",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "components/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**",
      "**/react-native-typescript-transformer"
    ]
  },
  "scripts": {
    "updatedependence": "lerna clean && yarn",
    "bootstrap": "lerna bootstrap",
    "startweb": "lerna run --scope @iwann/web start",
    "startmobileserv": "lerna run --scope @iwann/mobile start",
    "startapp": "lerna run --scope @iwann/mobile android"
  }
}
