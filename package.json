{
  "dependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "name": "@iwann/punkbeer",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "components/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**",
      "**/react-native-typescript-transformer"
    ]
  },
  "scripts": {
    "startweb": "lerna exec --scope @iwann/web -- yarn start",
    "startmobileserv": "lerna exec --scope @iwann/mobile -- yarn start",
    "startapp": "lerna exec --scope @iwann/mobile -- yarn android"

  }
}
