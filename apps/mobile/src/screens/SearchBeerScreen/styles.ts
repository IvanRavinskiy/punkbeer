import { StyleSheet } from "react-native";

export const SearchBeerScreenStyles = StyleSheet.create({
  SliderContainer: {
    padding: 20,
  },
  SliderValues: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingTop: 10,
  },
  image: {
    width: 21,
    height: 80,
  },
});
