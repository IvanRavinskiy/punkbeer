import { StyleSheet } from "react-native";

export const RandomBeerScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  imageContainer: {
    width: 150,
    height: 600,
  },
});
